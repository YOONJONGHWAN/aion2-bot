services:
  - type: web
    name: aion2-bot
    runtime: docker # 이 부분이 중요합니다!
    plan: free
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: DISCORD_CHANNEL_ID
        sync: false
